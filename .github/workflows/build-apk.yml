name: Build & Release Android APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Ensure Gradle Wrapper
        run: |
          if [ ! -f "gradlew" ]; then
            echo "Generating Gradle Wrapper..."
            gradle wrapper --gradle-version 8.4
          fi
          chmod +x gradlew

      - name: Build Debug APK with Gradle
        run: ./gradlew assembleDebug --no-daemon --stacktrace

      - name: Upload Debug APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SmartRemote-Pro-v1.0.0-Debug
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30

      - name: Create or Update GitHub Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.0-apk
          name: "SmartRemote Pro v1.0.0 (Free Android APK)"
          body: |
            ## 📱 SmartRemote Pro v1.0.0 - Android App Download
            
            🎉 **100% Free Android APK Ready to Install!**
            
            ### ✨ Features:
            - 📺 Universal Remote (TV, AC, Soundbar, Airtel Xstream)
            - 🎙️ Hindi + English Voice Control
            - 💻 Laptop / PC Wireless Mouse (👆 Finger Move + 📱 Mobile Move + 📊 PPT Clicker)
            - ⚡ Zero-Lag UDP & Bluetooth HID
          files: app/build/outputs/apk/debug/app-debug.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
